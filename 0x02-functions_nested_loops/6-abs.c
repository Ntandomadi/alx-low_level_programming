#include "main.h"

/**
 * int_abs(int) - computes the value of an integer
 * 
 * Return: absalute value of number or zero
 */

int _abs(int)
{
	if (c < 0)
	{
	int abs_val;

	abs_val = c * -1;
	return (abs_val);
	}
	return (c);
}
